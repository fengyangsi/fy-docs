#import "../fy-spec/lib.typ": *

= server 模块：本地服务与增量刷新

`server` 以 Axum 服务 `docs/target/`，默认从 `127.0.0.1:8181` 开始绑定；端口被占用时依次尝试后续端口。它不提供公网监听或部署能力。

== 实时刷新

`watcher` 递归监听 `docs/` 及自动探测到的本地 Typst 包目录。仅 `.typ` 文件变更会触发构建；500 ms 防抖将一次保存产生的事件合并。

每次成功触发后，构建号写入 `docs/target/_build`。服务模式下的 `_poll.js` 轮询该值，发现变化后刷新当前页面；静态构建的同名脚本为空，因此 `file://` 阅读不会发起网络请求。

#contract[
  编译错误不能让服务退出。工具必须生成一个可见错误页，保留后续源码变更触发的自动恢复能力。
]

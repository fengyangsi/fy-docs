#import "/fy-spec/lib.typ": *

= viewer 模块：原生阅读器交互

`viewer` 是内嵌到 `fy-docs.js` 的浏览器脚本。HTML、CSS 与 JavaScript 均由 Rust 二进制直接写入生成目录。

== 交互契约

+ 顶部工具栏始终位于侧栏外：左侧依次是目录开闭、主题与搜索；右侧是打印当前章节，以及在 `Cargo.toml` 声明 GitHub 仓库时显示的 GitHub 链接。
+ 主题选择、搜索与目录互不依赖；关闭侧栏不会隐藏全局工具。搜索按一级章节匹配正文，选择结果后进入对应章节。
+ 打印按钮调用浏览器打印当前章节；网页不提供 PDF 链接，PDF 仅由显式构建命令写入 `docs/release/`。
+ Typst 输出的目录移动到侧栏；点击目录小节后，阅读器显示其所属一级章节并定位到目标。
+ 每个 `h2` 对应一个阅读页，章节内部可滚动；页底“上一页 / 下一页”和左右方向键切换章节。

#example-box[
  主题与侧栏状态优先保存到浏览器本地存储。若隐私模式或 `file://` 禁止存储，阅读器退回默认状态，正文、目录与章节导航仍必须保持可用。
]
+ 顶部工具栏始终位于侧栏外：左侧依次是目录开闭、主题与搜索；右侧是打印当前章节，以及在 `Cargo.toml` 声明 GitHub 仓库时显示的 GitHub 链接。
+ 主题选择、搜索与目录互不依赖；关闭侧栏不会隐藏全局工具。搜索按一级章节匹配正文，选择结果后进入对应章节。
+ 打印按钮调用浏览器打印当前章节；网页不提供 PDF 链接，PDF 仅由显式构建命令写入 `docs/release/`。
+ 阅读器控件按文档内容自动选择中文或英文；英文文档中的画笔按钮提示为 `Theme`，不会出现中文工具提示。
+ 侧栏右侧分隔条可拖动调节宽度；宽度保存在浏览器本地。左右方向键始终用于章节翻页。侧栏始终保留纵向滚动条，窄视口的抽屉侧栏不提供宽度调节。

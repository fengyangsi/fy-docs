#import "../../fy-spec/lib.typ": *

= server 模块：Axum 开发工作台、源码监听与共享状态 <sec-server>

本章规定 `src/server.rs`，以及折叠进它的两个支撑模块：把源码编辑变成重建的 `src/watcher.rs`，和在 dev 进程各线程之间共享的状态对象 `src/state.rs`。HTTP 界面只在 `cargo fy-docs dev` 下存在；状态类型则位于所有编译类命令的路径上，因为 `compiler::generate` 要从它那里读取捕获的启动选项。

#contract[
  路由器只有一个路由：`GET /events`，一条 SSE 流。其余请求一律由 `docs/target/` 下的静态文件应答，没有生成物的路径就是普通的未找到。任何请求都不得触发生成，服务器只暴露某次生成已经写好的东西。
]

#contract[
  `dev` 的启动顺序固定：首次生成、派生监听、构造路由、绑定端口、可选打开浏览器、开始服务。监听套接字只在首次生成返回后才打开，于是缓慢的首次编译绝不会留下一个正在接受不存在页面请求的端口。首次生成失败同样要把服务器拉起来——浏览器看到的正是它刚写出的错误页。
]

#contract[
  构建编号是从 `1` 起的单调计数器，由一条广播通道承载；每个 `/events` 订阅各得一个接收端，所有订阅者看到的取值序列相同。该流不会失败，并保留默认心跳，使空闲页面仍是一条活连接。
]

#contract[
  新订阅的页面第一帧收到的就是当前构建编号，该帧即其基线；此后的每一次递增推出一帧携带新编号的数据。正因为要发这一帧基线，页面打开后的第一次保存才会立刻刷新：只报变化的流会让每个页面永远落后一次重建。
]

#contract[
  每一次生成尝试之后都要推进构建编号，无论它成功还是失败。失败的重建同样必须推出新编号，否则打开的页面会继续停留在上一个好版本，永远不知道源码已经写坏。
]

#invariant[
  状态对象在整个进程生命周期内额外持有一个接收端，唯一目的就是让广播通道保持打开。没有接收端的通道即为关闭，此后每条更新都会被静默丢弃——设置这个锚字段，是因为另一种结局是一个悄悄停止刷新的开发服务器。
]

#contract[
  监听范围是递归的 `docs/` 目录，外加项目解析出的每个本地导入的顶层目录，去重。`.typ` 文件的变化算源码变化；`target/` 或 `release/` 下的变化不算，即使那里出现以 `.typ` 结尾的产物也必须忽略，因为监听器绝不得对自己的输出作出反应。这些目录之外没有任何东西能触发重建。
]

#contract[
  一阵变更合并为一次重建：变更持续到达时监听器一直排空，但合并受两道边界约束——每次保存都会滑动的 500 ms 静默期，以及自首次变更起算的 2 s 硬上限。只有静默期的话，一个持续写入源码的进程就能把重建无限推迟。
]

#contract[
  失败的重建被丢弃，监听循环回到等待；循环只在通知通道关闭时结束。dev 会话靠错误页存活，而不是靠进程存活。panic 只在并行编译步骤内被收集成失败（见 `compiler` 章），监听线程本身没有 panic 屏障，因此重建中别处的 panic 会使该会话不再重建。
]

#contract[
  `ctrl-c` 经由优雅关闭路径停止服务，打印一行关闭信息后让进程退出；监听线程是普通线程，随进程一起结束，从不被 join。
]

#contract[
  一次性静态构建同样携带 `live.js` 客户端，此时连接 `/events` 必然失败而该失败被吞掉：以 `file://` 打开的页面不得报出它用不上的刷新错误。
]

== 模块结构

#logic-box[
  `server::router(state: &Arc<AppState>) -> Router` 构造这一单一路由加静态兜底的界面，并在构造时就取走一个订阅句柄。
  `watcher::spawn(state: Arc<AppState>) -> Result<()>` 注册全部监听，后台线程跑起来即返回；无法监听的目录是 `dev` 的错误，在任何端口绑定之前就抛出。
]

#logic-box[
  `AppState { project: Project, build_id: AtomicU64, generate: GenerateOptions, events:
  watch::Sender<u64>, _events_anchor: watch::Receiver<u64> }`——发送端与锚接收端均为私有；进入它的唯一途径是
  `AppState::with_generate(project, generate)`、`subscribe()`、`current_build_id()` 与
  `bump_build()`。`GenerateOptions { with_pdf: bool, lang_filter: Option<String> }`
  即捕获的启动选项；计数器与广播值只由 `bump_build` 一处保持同步，这正是所服务的编号与所打印的构建号能够一致的原因。
]

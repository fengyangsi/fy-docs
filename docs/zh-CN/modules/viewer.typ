#import "../../fy-spec/lib.typ": *

= viewer 模块：浏览器阅读器、主题与热重载 <sec-viewer>

`viewer` 章拥有三个文件——`assets/viewer.js`、`assets/base.css`、`assets/live.js`——它是唯一一章其代码绝不在 fy-docs 进程内运行。`page` 章把这三个文件内嵌为字符串常量，`compiler` 把它们写到每个生成页面的旁边，浏览器再从 `docs/target/` 加载它们。因此本章规定的是运行期行为，不是 Rust API。

三个文件逐字节原样交付：仓库与浏览器之间没有任何打包器或转译器，审阅者读到的源码就是用户跑起来的产物。它们所绑定的外壳是 `assets/doc.html`，由 `page` 章规定；该外壳声明的 `id` 与 `class` 集合是内部不变量，因此 `viewer.js` 有权查询其中任何一项，而不必为元素缺失设防。

#contract[
  `viewer.js` 在加载时重排文档：`#doc-body` 内每个顶层 `<h2>` 开启一个章节，首个标题之前的节点构成前置的封面章，typst 输出在正文里的 `nav[role="doc-toc"]` 被移出到侧栏。每章成为一个承载其节点的 `section.fy-chapter`，其锚点为首个节点的 `id`——该节点没有 `id` 时，前置章合成为 `cover`，其余合成为 `ch-<n>`。
]

#contract[
  章节内的每一个 `id` 都映射到该章节，于是站内锚点——目录条目、交叉引用、来自另一语言页面的 `#` 链接——总会打开包含其目标的章节。未映射的锚点交由浏览器自行处理。同一份映射负责解析初始 hash 与其后每一次 `hashchange`；提供上一页、位置与下一页的翻页条只为章节数大于 1 的文档构建。
]

#contract[
  侧栏跟随的是读者的位置，不是最后一次点击。参与者只有当前可见章节自己的标题，且必须同时带有 `id` 与对应的目录条目；指向本章之外的条目绝不会被点亮。真正滚动的 `.fy-content` 上挂一个被动（passive）滚动监听驱动重算，每帧合并为一次，窗口尺寸变化时同样重算，按下列三条顺序确定的规则：可见区顶边之上的最后一个标题；一个都未到达顶边时取第一个；容器滚到底时取最后一个——篇幅太短、永远到不了顶边的末节，全靠这一条才可达。切换章节时按新章的标题重建参与者表。
]

#contract[
  两个层级同时点亮：激活标题自身的条目挂 `fy-toc-active`，包含它的章条目挂 `fy-toc-chapter-active`，于是读到 `3.2` 时「3」仍作为所属章保持标记。当激活标题正是本章的开篇标题时，同一个条目承载两个类名，而显示出来的是位置类。目录只有一级时，退化为仅 `fy-toc-active`。
]

#contract[
  左、右方向键在章节间移动，除非该事件已被处理、携带修饰键，或来自文本输入框、textarea、select 与可编辑节点。
]

#contract[
  两项界面状态会被持久化——侧栏的开合标志与其宽度——二者都经由吞掉一切存储失败信号的访问器读写，使被拒绝的 `localStorage`（隐私模式、`file://` 页面）退回到默认值而不是抛错。视口宽度不小于 1024 px 时侧栏默认展开，其宽度被夹在 192 px 与 512 px 之间且不超过窗口的 55%，并由把手的指针捕获拖动。
]

#contract[
  主题是根元素上的单个类名，取 `light`、`rust`、`coal`、`navy`、`ayu` 之一。菜单中的系统项存储 `preference`，在着色时经由 `prefers-color-scheme` 解析——暗色即 `navy`，其余即 `light`——并且当选中该项而系统切换时重新着色。菜单以 `aria-checked` 标记存储的选项。
]

#contract[
  `base.css` 除调色板之外，还为 typst 烘焙进导出图形 SVG 的字面十六进制颜色提供逐主题覆盖，于是一幅内嵌的 DAG 会跟随当前主题，而不是停留在模板的浅色底上。
]

#contract[
  行长是测量长度（measure），不是窗口的一份。视口窄于 1280 px 时文档是一根居中的列，上限为 `--fy-page-max`——820 px，其两侧各 32 px 的内边距留下 756 px 的 `--fy-measure`。自 1280 px 起页面盒放宽到 `--fy-page-wide`（1180 px），自 1800 px 起放宽到 `--fy-page-ultra`（1440 px），而文档的每个顶层块仍被钉在同一个 756 px 测量长度上；唯有代码块、figure（typst 为表格与图示提供的外壳）与裸出的导出 SVG 从中放开，于是宽屏买到的宽度归给那些原本会横向溢出的材料，而不是被拉长的正文。这条上限为该流形的两种形态各写一遍——typst 直接输出在 `#doc-body` 下的块，以及章节重排把它们归入 `section.fy-chapter` 之后的同一批块——并且它绝不约束章壳本身，因为被钉在测量长度上的章壳，会把刚刚从中放开的材料再裁回去。
]

#contract[
  搜索是以章节为单位索引渲染后的文本，使用带区域信息的 `toLocaleLowerCase` 折叠大小写。每章只在其首个命中处报告一条结果，摘要取命中点前 15 个字符、后 35 个字符并把空白折叠，结果容器是 polite 实时区域，因而被替换的列表会被朗读。激活一条结果会关闭面板并跳转到该章锚点。
]

#contract[
  搜索摘要与章节标题在进入面板之前先做 HTML 转义，查询串则先做正则元字符引用，再用于把命中片段包裹成 `<mark>`。
]

#contract[
  加载时每个 `.fy-doc pre` 都被包进一个复制容器。按钮读取该代码块的渲染文本并交给异步剪贴板，随后短暂显示已复制态；剪贴板 API 缺失时点击不做任何事。
]

#contract[
  `#fy-print` 调用浏览器自身的打印，顶栏标题与侧栏项目名都导航回封面章。
]

#contract[
  页面根元素的 `lang` 描述*正文内容语言*：取 `project` 为该语言目标解析出的内容标签，并按 BCP 47 形态规范化（`pt_BR` → `pt-BR`，基础子标签小写、地区全大写、文字子标签首字母大写）。该标签只来自声明，绝不从正文字形推断。
]

#contract[
  顶栏 chrome 文案仅有中英两套：生成期由 `page` 章的 `ui_text` 一次性选定，浏览器内 `viewer.js` 又按根 `lang` 的 `zh` 前缀重新推出同一决定。正文语言与 chrome 语言互不干涉——未被翻译的语言以英文 chrome 呈现，根标签仍如实标注该语言。
]

#contract[
  语言项保留阅读位置：所选目标的文件名存入 `fydocs-lang`，且当前 URL 携带 hash 时该点击被拦截并改写为另一份文件中同一锚点。
]

#contract[
  `live.js` 在相对地址 `events` 上打开一个 `EventSource`，把第一帧记录为基线构建，此后任何取值不同的帧都触发刷新。流错误会永久且静默地关闭连接，这正是同一段脚本在无服务器的静态构建里不产生任何可见故障的原因。该脚本以不带 `defer` 的方式加载，因而订阅始于阅读器完成初始化之前。
]

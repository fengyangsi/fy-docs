#import "../../fy-spec/lib.typ": *

= page 模块：页面装配与转义 <sec-page>

`page` 模块负责装配阅读页：它持有标记外壳、界面文案表与转义阶梯，把一份编译完成的文档正文变成浏览器打开的那个文件。它工作在生成期。`compiler` 模块拥有 typst 进程以及产物落盘的文件名；`viewer` 模块拥有稍后在浏览器里运行于本模块产出的标记之上的代码。

#contract[
  `assets/doc.html` 是外壳，渲染就是在它之上做占位符填充。占位符集合恰好为：`TITLE`、`NAME`、`LANG`、`SIDEBAR_TOGGLE`、`THEME`、`SYSTEM_THEME`、`SEARCH`、`SEARCH_DOCUMENT`、`SEARCH_PLACEHOLDER`、`PRINT`、`TABLE_OF_CONTENTS`、`GITHUB_LINK`、`LANG_MENU`、`BODY`。
]

#contract[
  渲染出的页面不得残留任何 `{{` 序列。外壳每引入一个占位符，必须同时引入填充它的那次替换，两个文件不得彼此脱节。
]

#contract[
  每个占位符按它所在上下文要求的转义器写入：`TITLE` 与 `NAME` 做 HTML 文本转义，`LANG` 取 `project` 模块已经规范化好的该目标内容语言，`BODY` 原样插入编译产物，界面文案类占位符取自固定的内部表而非任何输入。`GITHUB_LINK` 与 `LANG_MENU` 是本模块自行构建的标记，已在叶子上完成转义。
]

#invariant[
  `assets/doc.html` 由 fy-docs 产出，绝不接受使用者提供。它声明的 `id` 与 `class` 集合因此是内部不变量，而不是可选输入：外壳缺失或改动了其中任何一个，都是本模块的缺陷，`assets/viewer.js` 有权无条件绑定它们中的每一个。
]

#contract[
  首屏主题由外壳内的内联脚本决定，早于任何外链样式表与脚本加载，页面不得闪一下错误配色：已存主题直接生效，未设置或遗留取值回落到跟随系统，并且在加上新类之前先清掉外壳所能点名的每一个主题类。
]

#contract[
  生成的页面相对于自己的输出目录完全自包含：它发出的每个 `href` 与 `src` 都只指向同级文件名，既不触及网络源也不使用绝对路径。因此 `file://` 与 dev 服务必须渲染出完全一致的结果；唯一的例外是仓库外链，它是导航目标而非资源依赖。
]

== 界面文案

#contract[
  工具栏界面只有中文与英文两套，由内容语言这一个纯函数的输入决定选哪套：规范化以后以 `zh` 开头取中文，其余一切——包括空串，以及葡萄牙语、日语这类尚未翻译的语言——取英文。文档正文从不被检视，所以字形无权决定界面语言。
]

#logic-box[
  `ui_text(content_lang: &str) -> UiText` 返回一个 `&'static str` 字段的结构体，覆盖侧栏开关、主题、跟随系统、搜索（标签、动作、输入提示）、打印、目录、GitHub、语言切换，以及编译失败的三条文案。同一个解析结果同时也是页面的 `lang` 属性，这正是"一个标签不可能与它旁边的文案互相矛盾"的原因。
]

#contract[
  未翻译的语言戴着英文界面帽，但它的根 `lang` 仍然如实命名自身：两者按设计是互相独立的轴，缺一门翻译绝不允许把一份文档改口说成另一门语言。
]

#contract[
  `目录` 这个标签在本模块（侧栏标题）与 fy-spec 模板（印刷封面）各存在一次，且中文值在模板侧带一个排版空格（`目 录`），这是刻意的：两者属于不同表面、遵循不同排版要求，不要求相等。
]

== 语言切换与落地页

#contract[
  语言菜单只在项目声明了至少两个具名语言目标时才渲染；单语言项目根本不产出切换器。被勾选的条目是当前目标，而默认目标（根 `docs/main.typ`）把第一个具名语言标为激活——因为那正是切换器自身顺序所呈现的那一页。
]

#contract[
  只有当清单声明了一个 GitHub 仓库 URL 时才发出 `GitHub` 图标链接；任何其他主机，以及没有声明，都在该位置产生零标记。
]

#contract[
  路由落地页是一段客户端跳转，除跳转脚本、语言映射表和一个朴素链接列表外不携带任何内容。解析顺序为：阅读者显式存下的语言优先；其次依次取浏览器语言列表的每一项，先按完整标签匹配、再按基础子标签匹配；最后是默认目标，默认目标的偏好顺序为英语、其次简体中文、其次第一个具名目标。`<noscript>` 的刷新指令镜像同一个默认值，因此禁用 JavaScript 时该页依然完成路由。
]

#contract[
  落地页的语言映射表以规范化小写的完整标签及其基础子标签为键，两者都指向该目标的 HTML 文件名，于是 `zh-cn` 与 `zh` 都能到达 `zh-CN` 目标。落地页只为至少拥有一个具名语言目标的项目生成。
]

== 转义阶梯

#logic-box[
  三个转义器，各对应一种上下文，且脚本上下文那个定义在其余两个之上：
  - 文本——`&`、`<`、`>`；用于元素内容。
  - 属性——文本转义再加 `"`；用于带引号的属性值。
  - 脚本字符串——属性转义并不够，所以它先做 HTML 元字符转义，之后才做 JavaScript 字面量转义：反斜杠、包裹用的那个引号、`\n`、`\r`、`\t`、`U+2028`、`U+2029`，以及一切低于 `U+0020` 的字符。引号形态有两个变体，分别服务双引号 JSON 与单引号字面量。
]

#contract[
  HTML 元字符必须先于 JavaScript 字面量字符转义，且这个顺序是承重的：先把 `</script>` 转成 `&lt;/script&gt;`，才使一个值无法终结 script 元素；之后再做的字面量转义则保证结果仍是一个合法字面量。
]

#contract[
  内联样式表、阅读器脚本与热重载客户端这三份资产的*字节*归本模块所有，在编译期内嵌；它们落盘时的*文件名*归 `compiler` 模块所有。任何一边都不得复制另一边的清单。
]

#contract[
  错误输出走的是与成功页面同一个外壳，于是一次失败的构建产出的仍然是有样式、有本地化文案、可导航的页面，而不是一篇裸文本：诊断信息按元素内容转义后放进正文槽位，而外壳的界面文案、语言切换器和资产链接照旧来自同一个目标。
]
